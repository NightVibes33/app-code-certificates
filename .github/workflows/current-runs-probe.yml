name: Current runs probe

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/current-runs-probe.yml"

permissions:
  actions: read
  contents: write

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Record current workflow runs
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh api "repos/${GITHUB_REPOSITORY}/actions/runs?per_page=20" | jq '[.workflow_runs[] | {id,name,status,conclusion,head_sha,event,created_at,html_url}]' > current-runs.json
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add current-runs.json
          git commit -m 'Record current workflow runs'
          git push
